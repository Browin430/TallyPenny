name: record_linux
description: Local no-op replacement for the official record_linux (desktop recording unused; keeps AOT build working).
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.0.0 <5.0.0"

dependencies:
  record_platform_interface: 1.3.0
